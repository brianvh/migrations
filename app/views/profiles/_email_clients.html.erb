<% EmailClientChoice.all.each do |choice| %>
	<%= check_box_tag("profile[used_email_choices][]", choice["value"],
										@profile.used_email_clients =~ Regexp.new(Regexp.escape(choice["value"])) ? true : false,
										options = {}) %>
	<%= choice["label"] %>
<% end -%>
