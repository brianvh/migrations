<%= form_for @group do |f| -%>
<div id="send-invitations" class="fieldset form" style="display: none">
  <h2>Compose and Send Invitation Email</h2>
  <div class="group navform wat-cf">
    <div class="text_button_padding">
      <%= hide_div_link 'Cancel', 'send-invitations' %>
    </div>
    <div class="right">
      <button class="button" type="submit">Send Invitations</button>
    </div>
  </div>
  <%= f.hidden_field :action, :value => :send_invitations %>
  <div class="group wat-cf">
    <div class="left label"><%= f.label :invite_msg, "Message" %></div>
    <div class="right">
      <%= f.text_area :invite_msg, :size => '80x10', :value => !@group.invite_msg.blank? ? @group.invite_msg :
      %{We need information about your current email and calendar accounts, your computer and other devices (including smartphones), and the software you use now and plan to use with (new) Blitz.  Please go to our web-based form and complete your profile; it will take five minutes:
} %>
    </div>
  </div>
</div>
<% end -%>
